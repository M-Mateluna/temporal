<?php /* index.php — Ejercicio Propuesto: Uso de nuevos estilos (Desis) */ ?>
<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=ISO-8859-1">
  <title>Gestión de Clientes / Proveedores</title>

  <!-- Import del nuevo estilo, tal como indica el documento -->
  <link type="text/css" rel="stylesheet" href="<?=$POSICION?>css/nuevoestilo.css?v=1.8">

  <style type="text/css">
    /* Ajustes mínimos sin reemplazar el nuevoestilo.css */
    .contenedor { max-width: 980px; margin: 10px auto; background: #f3f3f3; padding: 8px; border-radius: 6px; }
    .grid { width: 100%; border-collapse: collapse; }
    .grid td { width: 50%; vertical-align: top; padding: 6px 10px; }  /* espacio entre columnas */
    .stack label { display: block; margin: 0 0 4px 2px; }            /* label arriba del input */
    .stack input { width: 92%; box-sizing: border-box; }             /* ancho similar al ejemplo */
    .fila { height: 52px; }                                          /* separa verticalmente las filas */
  </style>
</head>
<body>

<div class="FORMULARIONECSS contenedor">
  <form id="frmCP" method="post" autocomplete="off" onsubmit="return false;">
    <fieldset>
      <legend class="legends">Información del Registro</legend>

      <!-- Maquetación con TABLAS según el ejemplo del documento -->
      <table class="grid" cellspacing="0" cellpadding="0">
        <tr class="fila">
          <!-- Columna izquierda -->
          <td>
            <table width="100%" cellspacing="0" cellpadding="0">
              <tr>
                <td class="stack">
                  <label for="rut">RUT</label>
                  <input type="text" name="rut" id="rut" maxlength="20" placeholder="12.345.678-9">
                </td>
              </tr>
            </table>
          </td>

          <!-- Columna derecha -->
          <td>
            <table width="100%" cellspacing="0" cellpadding="0">
              <tr>
                <td class="stack">
                  <label for="giro">Giro</label>
                  <input type="text" name="giro" id="giro" maxlength="120">
                </td>
              </tr>
            </table>
          </td>
        </tr>

        <tr class="fila">
          <!-- Columna izquierda -->
          <td>
            <table width="100%" cellspacing="0" cellpadding="0">
              <tr>
                <td class="stack">
                  <label for="idusuario">ID Usuario</label>
                  <input type="text" name="idusuario" id="idusuario" maxlength="10">
                </td>
              </tr>
            </table>
          </td>

          <!-- Columna derecha -->
          <td>
            <table width="100%" cellspacing="0" cellpadding="0">
              <tr>
                <td class="stack">
                  <label for="fechahora">Fecha/Hora</label>
                  <input type="datetime-local" name="fechahora" id="fechahora">
                </td>
              </tr>
            </table>
          </td>
        </tr>
      </table>

      <!-- Botonera opcional (mismo estilo) -->
      <div style="padding:6px 10px;">
        <button type="submit" class="textazul">Guardar</button>
        <button type="reset"  class="textazul">Limpiar</button>
      </div>
    </fieldset>
  </form>
</div>

<script type="text/javascript">
  // Precargar Fecha/Hora sin romper el estándar
  (function(){
    var fh = document.getElementById('fechahora');
    if (fh && !fh.value) {
      var d = new Date(); d.setMinutes(d.getMinutes() - d.getTimezoneOffset());
      fh.value = d.toISOString().slice(0,16); // YYYY-MM-DDTHH:MM
    }
  })();
</script>

</body>
</html>
